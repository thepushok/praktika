
int Proizv(vector<int>a,int n) 
{ 
 int proiz = 1; 
 for (int i = 0;i < n;i++) 
 { 
  proiz *= a[i]; 
 } 
 return proiz; 
} 

int Kol_vo_delit(int pr)
{
	int count = 1; 

	for (int i = 2; i <= pr; i++) 
	{
		if (pr % i == 0) 
		{
			count++; 
		}
	}
	return count;
}
